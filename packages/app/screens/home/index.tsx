import React from "react";
import { SafeAreaView } from "app/provider/safe-area";

export function HomeScreen() {
  return (
    <SafeAreaView
      style={{
        flex: 1,
      }}
    ></SafeAreaView>
  );
}
