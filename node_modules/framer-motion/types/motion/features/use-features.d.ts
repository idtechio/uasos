/// <reference types="react" />
import { VisualElement } from "../../render/types";
import { MotionProps } from "..";
import { MotionFeature } from "./types";
/**
 * Load features via renderless components based on the provided MotionProps.
 * TODO: Look into porting this to a component-less appraoch.
 */
export declare function useFeatures(defaultFeatures: MotionFeature[], visualElement: VisualElement, props: MotionProps): null | JSX.Element[];
