import { MotionProps } from "../../motion";
import { CreateVisualElement, VisualElement } from "../../render/types";
import { VisualState } from "./use-visual-state";
export declare function useVisualElement<Instance, RenderState>(isStatic: boolean, visualState: VisualState<Instance, RenderState>, createVisualElement: CreateVisualElement<Instance>, props: MotionProps): VisualElement<Instance>;
